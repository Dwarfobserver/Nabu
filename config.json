
{
    "pre_build":
    {
        "debug_build": true,
        "build_directory": "build",
        "merge_client_server": false,
        "client_logger_verbosity": "status",
        "server_logger_verbosity": "status"
    }
}
